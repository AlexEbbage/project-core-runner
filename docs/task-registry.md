# Task Registry

## Working Rule

Break approved features into tasks labeled `F#-T#`. Update status as the work changes. This file is the immediate backlog, not a speculative dump.

## Task Tracking Table

| Task | Feature | Description | Owner | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| F1-T1 | F1 | Restore the visible Play route, add a development Quick Play route, and prove the core run lifecycle in the live scene | Codex/User | Implemented (awaiting review) | Pointer input starts the selected run, the editor command starts a known valid run, and three PlayMode smoke tests cover scene uniqueness, listener wiring, duplicate start rejection, non-overlapping Game Over actions, real Retry/Menu callbacks, distinct run sessions, Home, and Play after Home. Human control/lifecycle signoff remains. |
| F1-T2 | F1 | Generate a route-configured recycling tunnel and keep the gameplay camera behind and roll-aligned with the player | Codex/User | Implemented (awaiting review) | The default route generates 48 six-sided longitudinal sections, recycles the mesh ahead of the player, preserves the authored FBX as an edit-time source visual, and keeps the craft upright in frame while steering makes the tunnel appear to rotate. |
| F1-T3 | F1 | Correct bottom-rail orientation and apply the first camera/control/readability tuning pass | Codex/User | Implemented (awaiting review) | Runs now start at 270 degrees on the bottom rail with an upright camera, hold the ship at viewport Y 0.411, use 140 degrees/second steering, apply a mid-dark tunnel tint without modifying the shared material, and clear stale thruster trail positions after the start teleport. Human comfort signoff remains. |
| F10-T1 | F10 | Author a checked-in daily login reward config and move claim cadence persistence into `PlayerProfile` | Codex/User | Implemented (awaiting review) | Covers streak day progression, single-claim enforcement, reward preview state, and runtime config loading through `Resources` |
| F10-T2 | F10 | Route daily login access through the hub/tasks shell without auto-claiming on open | Codex/User | Implemented (awaiting review) | Keeps the flow manual and preview-driven inside the current shell |
| F10-T3 | F10 | Add an optional rewarded `Claim x2` branch that doubles the daily-login reward without auto-fallback | Codex/User | Implemented (awaiting review) | Reuses the existing rewarded-ad readiness path and keeps normal claim available when the ad path fails |
| F11-T1 | F11 | Productize the current progression task UI/config into a claimable daily/weekly/monthly task flow | Codex/User | Implemented (awaiting review) | Covers action-button states, persisted claim state, cadence-level refresh, and runtime config loading through `Resources` |
| F11-T2 | F11 | Move task and milestone reward ownership into `PlayerProfile` and grant profile rewards through a shared path | Codex/User | Implemented (awaiting review) | Uses profile-backed cadence state instead of transient UI-only state |
| F11-T3 | F11 | Expand cadence groups into deterministic randomized task pools that persist active picks for the current cycle | Codex/User | Implemented (awaiting review) | Daily and weekly now draw 3-of-6 tasks, monthly draws 3-of-5, and active ids persist in `PlayerProfile` |
| F11-T4 | F11 | Drive task progress from real run-end results instead of static authored preview values | Codex/User | Implemented (awaiting review) | Supports runs, distance, score, coins, XP, and no-continue completions through the shared profile owner |
| F13-T1 | F13 | Finalize rewarded ad placement policy across continue flow, reward doubling, and side prompts | Codex/User | Completed | Mid-run rewarded offers are now wired into the run loop with interval timing and weighted rewards |
| F13-T2 | F13 | Validate weighted offer rewards, interval pacing, and pause/resume behavior against gameplay states | Codex/User | Implemented (awaiting review) | Covers popout timing, modal flow, ad outcomes, and reward grant correctness |
| F1-T4 | F1 | Add readable VFX feedback for pickups, collisions, shield state, crash dissolve, continue warp, and speed | Codex/User | Implemented (awaiting review) | Existing pooled VFX are routed from gameplay events; a PlayMode smoke test covers damage, shield, and speed feedback |
| F1-T5 | F1 | Route music and SFX through the core run lifecycle and gameplay feedback events | Codex/User | Implemented (awaiting player review) | Menu/run music, transitions, pickups, obstacle passes, damage/death, shield state, and speed tiers use the existing checked-in clips through a bootstrap-owned audio host |
| F6-T2 | F6 | Improve portrait HUD hierarchy, active-powerup readability, pause presentation, and safe-area behavior | Codex/User | Implemented (awaiting player review) | Live screenshots confirm labelled dark-on-light metrics and a full-screen pause overlay; EditMode covers safe-area math and PlayMode proves authored references plus pause-safe powerup timers |
| F2-T1 | F2 | Restore authored wedge, fan, and door obstacles with grouped placement and time/route-scaled difficulty | Codex/User | Partially implemented (manual collider adjustment pending) | Five clean generated prefabs replace the crossed cube blocks and fit the tunnel at scale 4/7 with a -30-degree correction; the one-piece wall still needs the user's planned manual collider adjustment |
| F2-T2 | F2 | Tune obstacle group readability, reaction spacing, moving-obstacle timing, and retry reset | Codex/User | Implemented (awaiting player review) | Groups now hold one lane for 2–3 rings, consecutive groups change hex sides, early spacing is 13.8 units, fan/door timing is slower, and pooled motion state resets cleanly |
| F6-T1 | F6 | Centralize game-over presentation data and bind the existing game-over screen to resolved run stats and rewards | Codex/User | Implemented | Live MCP and PlayMode checks confirmed base wallet/XP settlement, summary presentation, Retry, and Home |
| F4-T1 | F4 | Gate continue behind a short game-over timer and keep revive flow separate from double rewards | Codex/User | Implemented | Development rewarded fallback completed Continue; the run resumed at normal time scale with no reward settlement until final end |
| F13-T3 | F13 | Make the game-over `x2 Rewards` path one-shot and prevent duplicate bonus grants across ad callbacks | Codex/User | Implemented | Live checks confirmed bonus wallet/XP deltas and disabled button state after one successful claim |
| F7-T1 | F7 | Remap the existing main-menu pages into the target hub destinations and make `Level Select` the default landing page | Codex/User | Implemented (awaiting review) | Covers bottom-nav routing for shop, ship, lab, levels, and achievements |
| F7-T2 | F7 | Add side-entry hub actions and badge visibility for daily login, special offers, tasks, and notifications | Codex/User | Implemented (awaiting review) | Keep badge state lightweight and sourced from existing managers/controllers |
| F8-T1 | F8 | Keep the hub top bar in sync with profile level, XP, and currency state | Codex/User | Implemented (awaiting review) | Currency taps should continue routing into the shop currency surface |
| F8-T2 | F8 | Roll run XP into level progression, unlock feedback, and persisted level-select state | Codex/User | Implemented | XP now advances across multiple thresholds with remainder preservation, queues level-up feedback, persists the selected route, and is covered by EditMode regression tests |
| F9-T1 | F9 | Author a checked-in shop catalog and align it with ship, skin, and trail unlock ids already used by profile and hangar systems | Codex/User | Implemented (awaiting review) | First slice covers one meaningful item path per visible shop tab plus default owned content |
| F9-T2 | F9 | Harden shop browse and modal purchase flow so owned, purchasable, insufficient-funds, and premium-action states are distinct | Codex/User | Implemented (awaiting review) | Covers card labels, modal state messaging, purchase persistence, and hangar refresh after unlocks |
| F14-T1 | F14 | Keep the currency tab scoped to the existing `Remove Ads` entitlement and restore flow without adding new live IAP products | Codex/User | Implemented (awaiting review) | The first production shop slice intentionally avoids a second premium-commerce architecture |
| F17-T1 | F17 | Reframe the current hangar shell into a clearer ship customisation surface with preview-first equip flow | Codex/User | Implemented (awaiting review) | The hangar now exposes a customisation-first tab row and current-loadout preview while keeping upgrades as a secondary surface |
| F17-T2 | F17 | Make equip state, lock state, and preview refresh obvious across the visible customisation categories | Codex/User | Implemented (awaiting review) | Covers ships, skins, trails, and core FX plus shop-to-hangar refresh and gameplay cosmetic handoff |
| F12-T1 | F12 | Turn the play page into a persisted level-select surface with readable lock states and selection cards | Codex/User | Implemented | Reuses the existing play page; PlayMode and live scene checks confirmed five cards and selection persistence |
| F12-T2 | F12 | Use profile level thresholds to gate selectable levels and keep the selected run target in sync with the hub | Codex/User | Implemented | The chosen route survives returning to the hub and starts the corresponding configured run |
| F12-T3 | F12 | Keep five route cards over one stable MVP tunnel type and challenge-rich cards while deferring elemental presentation | Codex/User | Implemented | Profile gates and route persistence remain; all routes intentionally share the neutral two-tone tunnel, red hazards, and authored orange VFX for MVP |
| F18-T1 | F18 | Keep `Lab` as the dedicated hub destination for combo and powerup upgrades while separating it from ship loadout browsing | Codex/User | Implemented (awaiting review) | Current milestone preserves the hub route and refresh hooks and now ships the authored six-row lab presentation |
| F18-T2 | F18 | Finalize upgrade card clarity, authored tuning data, and runtime validation for combo plus the target powerup set | Codex/User | Implemented (awaiting review) | Purchase refresh is wired and the target roster is now locked to the combo plus five powerup upgrades |
| F19-T1 | F19 | Convert the current challenges destination into a first achievement shell with tier progress and claim states | Codex/User | Implemented (awaiting review) | Uses `AchievementsPageController`, authored `AchievementsConfig.asset` content, and runtime config loading through `Resources` |
| F19-T2 | F19 | Persist achievement tier claims and reward grants through `PlayerProfile` without adding a second save owner | Codex/User | Implemented (awaiting review) | Achievement rewards now use the same profile grant path as tasks and daily login |
| F19-T3 | F19 | Expand the authored achievement roster to cover the full profile metric set already tracked in repo | Codex/User | Implemented (awaiting review) | Adds breadth for profile level, unlocked items, upgrades, soft currency, and premium currency without new metric enums |
| F15-T1 | F15 | Expand analytics event contract for run outcomes, ad sources, progression, economy, and shop actions | Codex/User | Implemented (awaiting review) | Shared constants now cover run, ad, hub, shop, hangar, progression, and level-select telemetry; the remaining step is the live Unity review pass |
| F20-T1 | F20 | Add booster catalog data, profile-backed loadout persistence, and a visible pre-run selection surface | Codex/User | Implemented | The authored Level Select shell exposes three family-labelled options and persists the loadout in profile v3 |
| F20-T2 | F20 | Apply the selected booster loadout at run start and keep the modifiers scoped to the current run only | Codex/User | Implemented | Live and PlayMode checks confirmed start shield, x2 score, x2 coins, and reset to x1 on Home |
| F21-T1 | F21 | Wire the save-backed FTUE tutorial sequence into `CoreRacer_Main` using the existing tutorial service and overlay | Codex/User | Implemented (awaiting review) | Covers welcome, input, dodge, coin, powerup, continue explanation, Lab prompt, and daily task/reward prompt |
| F21-T2 | F21 | Add deterministic first-run tutorial assists plus support/debug reset and debug analytics validation | Codex/User | Implemented (awaiting review) | Coin/powerup opportunities are queued only for matching tutorial steps; reset and tutorial analytics now route through existing abstractions |
| F16-T1 | F16 | Adopt DOTween as the UI motion layer and define integration boundaries for authored panels, overlays, and badge feedback | Codex/User | Completed | DOTween is now installed and the reusable motion helper is the preferred integration path |
| F16-T2 | F16 | Apply motion to hub page transitions, feature-panel open/close, game-over, shop modal, and rewarded prompt surfaces | Codex/User | Implemented (awaiting review) | Motion hooks are present; live GameScene verification still needs the editor pass |
| F16-T3 | F16 | Add lightweight motion emphasis for bottom nav, top-bar currency/level feedback, and actionable badges plus verify the motion checklist | Codex/User | Implemented (awaiting review) | Emphasis hooks are present; closeout now depends on the runtime verification checklist |
| T1 | Docs Refresh | Complete docs scaffold population and set docs as authoritative project context | Codex | Completed | Done in this pass |
| T2 | Docs Refresh | Validate docs against repo truth and separate implemented vs partial vs planned status correctly | Codex | Completed | Done in this pass |
| T3 | Meta Roadmap | Converge progression, economy, and reward systems into a first approved implementation slice | Codex/User | Completed | The XP/levels and level-select slice is complete; broader progression-shell work remains tracked separately |
| T4 | Environment Roadmap | Complete themed environment unlock and environment-select product definition | Codex/User | Implemented (awaiting review) | The roadmap is authored as five themed environments with profile-level gates and display-only challenge goals over one stable tunnel type |
| T5 | UI Roadmap | Lock the UI tooling/polish direction and transition strategy | Codex/User | Completed | DOTween is the approved UI motion layer and the transition strategy is now captured in the style/decision docs |

## Notes

- Task status values: `Not started`, `In progress`, `Blocked`, `Implemented (awaiting review)`, `Completed`
- Use `F#-T#` when a task belongs to a specific tracked feature.
- Keep future work grouped under the feature that owns it whenever possible.
