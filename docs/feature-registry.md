# Feature Registry

## Working Rule

Do not implement a feature unless it appears in this file and the user has chosen it. Use the status values here to separate repo truth from roadmap intent.

## Product Goal

- Build and polish a complete run-based arcade experience with strong meta-progression.
- Prioritize the main loop in this order: `Run -> Rewards -> Progression -> Repeat`.
- Treat each experience feature as a combination of `Core System`, `UI`, `UX / Feel`, and optional `Debug Tools`.

## Experience Tracking Table

This table is the product-facing view of the target experience. The `Linked Repo Features` column maps each experience slice back to the stable `F#` identifiers used by the task and script registries.

| Priority | Loop Stage | Experience Feature | Linked Repo Features | Core System | UI | UX / Feel | Debug Tools | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Run | Powerups | F5, F18 | Five target powerups: `x2 Score`, `x2 Coin Spawn`, `Magnet`, `Autopilot`, `Shield`; time-based effects; shield can use hit-based durability; lab upgrades for duration, strength, and charges | HUD indicators for active powerups plus duration/progress bars | Clear pickup, active, and expiry feedback with immediate gameplay impact | Force-spawn powerups, grant upgrade levels, inspect active timers | Partial | Runtime powerups and HUD support exist, and the active roster has now been narrowed toward the target set. Upgrade content, authored visuals, and final lab presentation still need completion. |
| 2 | Run -> Rewards | Rewarded Offers (Mid-Run) | F6, F13 | Random timed offers during gameplay; limited response window; accept can pause gameplay and open an offer modal; ignore/timeout dismisses cleanly | Small popout with countdown timer and a modal showing reward, rewarded-ad CTA, and ignore action | Should feel visible but non-intrusive, with clean expiry behavior | Force-trigger offer, choose reward type, simulate ad-ready state | Partial | The interval trigger, weighted reward pool, runtime popout, and tapped modal flow are now wired in code. A scene-authored polish pass and user-testing verification are still pending. |
| 3 | Rewards | Game Over Screen | F4, F6, F13 | End-of-run state, run stats, rewards earned, optional `x2 Rewards` ad, bonus grant after ad completion, continue gating | Game-over panel with stats, rewards, combo modifier display, continue CTA, double-reward CTA, restart/menu actions | Ad decision should feel deliberate without being frustrating; buttons should retire cleanly after use | Force end-run state, simulate ad outcomes, inspect reward grant logs | Implemented (awaiting player review) | Base settlement, development rewarded Continue, one-shot Double Rewards, Retry, Home, and profile wallet/XP persistence are live-verified in `CoreRacer_Main`; production ad SDK readiness remains a separate release gate. |
| 4 | Repeat | Hub (Main Menu) | F7, F8 | Central navigation across progression systems and run entry points | Top bar for profile/currencies/settings, bottom navigation, side-entry icons, notification indicators | Clear entry points to rewards, upgrades, and play-again flow | Toggle page locks, badge states, notification visibility | Partial | `CoreRacer_Main` now has an authored clean hub shell with working routes for `Play`, `Shop`, `Hangar`, `Lab`, `Progression`, and `Settings`, plus nested level-select, daily login, tasks, achievements, comfort, privacy, and support/debug sections. Remaining work is UX polish and player-facing review rather than missing scene wiring. |
| 5 | Repeat -> Progression | Shop | F9, F14, F17 | Premium bundles, cosmetics, boosters, and currency packs with purchase, unlock, persistence, and entitlement handling | Scrollable sections, headered grids, rarity styling, item detail modal | Browse-to-buy flow should feel quick, clear, and monetisation-safe | Mock catalog loader, fake purchase mode, entitlement reset tools | Partial | The first production slice now has a checked-in `ShopDatabase.asset`, authored catalog content, and a `GameScene` route into the feature-panel-hosted shop flow. Remaining validation is manual runtime verification in Unity. |
| 6 | Progression | Ship Customisation | F9, F17 | Equipable ship parts with persisted loadout and unlock ownership | Ship preview, equip slots, tabbed grids for item categories | Selection should update the preview immediately and support quick comparison | Unlock/equip cheats, preview swapper, missing-art warnings | In progress | The hangar shell now has a preview-first tabbed loadout flow for `Ships`, `Skins`, `Trails`, `Core FX`, and `Upgrades`, with ship ownership aligned to the shop catalog and preview refresh routed through `PlayerCosmetics`. Remaining validation is the live Unity runtime pass. |
| 7 | Progression | Lab | F5, F18 | Upgrade powerups and combo modifier with scaling costs, persistent levels, and clear gameplay impact | Upgrade cards/list showing level, next effect, cost, and lock state | Upgrade paths should feel understandable and worth chasing | Currency grant shortcuts, upgrade-level injector, delta readout | Implemented (awaiting review) | The progression milestone now keeps lab entry in the hub, refreshes hub chrome after upgrade purchases, and separates customisation-first browsing from upgrade actions. The target roster is authored to the six upgrade rows in the current slice, with live Unity review still remaining. |
| 8 | Progression | Achievements | F19 | Multi-tier milestone challenges with escalating rewards and optional combo-modifier boosts | Achievement list with progress indicators, tier rewards, and claim states | Long-tail goals should feel motivating and readable | Seed progress, reset achievements, skip to tier tools | In progress | The current `Challenges` destination now has a dedicated achievement controller and a five-track milestone config with persistent tier-claim state. The config is runtime-loadable through `Resources`; live Unity validation is the remaining gate. |
| 9 | Progression | Tasks (Daily / Weekly / Monthly) | F11 | Time-based reset, randomized task selection, task-point economy, claim and claim-all flows, milestone reward track | Task list with rewards, cadence tabs, timers, claim actions, horizontal reward track | Reward availability should be obvious from the hub and inside the page | Time fast-forward, reroll tasks, force completion, claim-state reset | In progress | Task cadence state, deterministic 3-of-pool selection, run-result task progress, reward claiming, and hub refresh are now persisted through `PlayerProfile`. Live Unity review is the remaining gate. |
| 10 | Rewards -> Repeat | Daily Login Reward | F10 | Once-per-day claim, streak progression, milestone rewards, optional `Claim x2` rewarded-ad variant | Login modal plus manual access from the hub | Claim flow should be fast and satisfying, with obvious streak value | Clock/day skip tools, reward preview, claim simulator | In progress | A checked-in reward config asset now backs a profile-persisted daily claim loop with manual hub access and preview state. The optional doubled-reward ad branch is now wired through the existing rewarded-ad service path, and live Unity review is the remaining gate. |
| 11 | Repeat -> Run | Boosters | F20 | Pre-run booster selection with one active booster per family and per-run effects on survival, coins, or score | Booster loadout UI before play and active-state indication during runs | The benefit should be obvious without creating confusion or excessive friction | Grant booster inventory, log per-run effect deltas, disable-cost mode | Implemented (awaiting player review) | The clean-scene booster slice persists one choice per family, exposes the loadout on Level Select, and applies run-scoped shield, coin, and score effects. Unity MCP and PlayMode verification passed; player-facing clarity remains the review gate. |
| 12 | Progression | XP & Levels | F8 | XP gained per run, level progression, feature unlock gates, upgrade-slot/system unlocks | Top-bar level/XP display, lock states, level-up popup/feedback | Growth should feel steady and meaningful, with clear unlock anticipation | Add XP, force level, inspect unlock matrix | Implemented (awaiting player review) | Run XP rolls across multiple level thresholds with remainder preserved, the hub refreshes from profile progression, and the play page surfaces level-up feedback. Human portrait-device review remains for unlock clarity and pacing. |
| 13 | Repeat -> Run | Environment Select | F12 | One stable six-sided tunnel type with profile-gated route cards and three display-only challenges per route | Environment cards/details showing goals, locks, and play CTA | MVP gameplay uses one neutral presentation; elemental environment identities are deferred until after the core loop and progression are proven | Unlock-all toggle and route injector | Implemented (awaiting player review) | The play page preserves five persisted route cards and level gates, but all currently resolve to the same neutral two-tone hex tunnel, red obstacle family, and authored orange VFX. |
| 14 | Run -> Repeat | FTUE Tutorial | F21 | Save-backed first-time sequence for welcome, movement, dodge, coin, powerup, crash, and successful Continue | Tutorial overlay plus support/debug reset | First run should teach one action at a time without relying on random spawn timing | Reset tutorial, debug analytics logs, deterministic tutorial pickup assists | Implemented (awaiting player review) | The v3 gameplay tutorial no longer exits to meta pages mid-run. PlayMode proves it waits for an actual crash and only completes after Continue restores the run; portrait-device pacing review remains. |

## Canonical Repo Feature Map

These `F#` identifiers remain the stable feature IDs used by `task-registry.md` and `script-registry.md`.

| ID | Group | Canonical Feature | Status | Notes |
| --- | --- | --- | --- | --- |
| F1 | Core Gameplay | Endless Run Foundation | Implemented (awaiting player review) | Live PlayMode verification covers Play entry, camera/control framing, recycling tunnel, score/distance, crash/Game Over, Retry/Home, plus menu/run music and core pickup, obstacle, damage, shield, and speed audio feedback. |
| F2 | Core Gameplay | Obstacle and Pickup Generation | Implemented (awaiting player review) | Recovered wedge, fan, and door assets spawn in readable side-aligned groups with the corrected single-wall collider settings; pickups align to the same six-sided lane frame and moving patterns reset cleanly |
| F3 | Core Gameplay | Score, Combo, and Speed Loop | Implemented | Distance and pickup scoring, combo growth/decay, speed escalation |
| F4 | Core Gameplay | Continue and Rewarded Continue Flow | Implemented | Crash recovery with capped continues and rewarded ad usage |
| F5 | Core Gameplay | Powerup Gameplay Loop | Partial | Runtime powerups are present and the current slice has converged the roster, HUD support, and upgrade fallback behavior, but authored content polish is still pending |
| F6 | UI | In-Run HUD and Reward Presentation | Implemented (awaiting player review) | HUD now has labelled high-contrast score, distance, coins, hull, active-powerup timers, safe-area containment, and a full-screen pause state; crash/Game Over, Continue, Double Rewards, Retry, and Home remain runtime-proven. Human device review remains. |
| F7 | UI | Main Hub Navigation | Partial | The clean `CoreRacer_Main` scene now carries the authored hub shell and passes scene/localization/product-catalog validation; remaining work is layout polish, content review, and broader UX signoff |
| F8 | Meta | Currency, Profile, XP, and Account Progression | Implemented (awaiting player review) | Currencies and profile persistence exist; XP rollover, level feedback, and level-select gating are covered by focused tests and live-scene wiring. Human review remains for unlock clarity and pacing. |
| F9 | Meta | Shop, Hangar, and Customisation Basics | Partial | Shop catalog assets, purchase states, and a live menu entry path are now wired; runtime/manual verification is the remaining gate for this slice |
| F10 | Meta | Daily Login Rewards | In progress | Checked-in reward config, profile-backed claim cadence, manual hub entry, and rewarded `Claim x2` support are now implemented through the existing service path; live Unity validation remains |
| F11 | Meta | Tasks and Milestone Rewards | In progress | Live cadence state, deterministic randomized task pools, real run-result task progress, claiming, and reward grants are now wired through `PlayerProfile`; live Unity validation remains |
| F12 | Meta | Environment Unlocks and Environment Select | Implemented (awaiting player review) | Five persisted route cards, profile level locks, and display-only goals remain, while MVP gameplay intentionally uses one fixed neutral tunnel/obstacle/VFX presentation |
| F13 | Services | Ads and Reward Monetisation | Partial | Continue and Double Rewards are live-verified with the development fallback provider; production LevelPlay readiness and mid-run offer review remain separate gates |
| F14 | Services | Premium Commerce | Partial | Remove-ads exists; broader premium catalog is planned |
| F15 | Services | Analytics Coverage | Implemented (awaiting review) | Shared event names and normalized payload keys now cover run outcomes, ads, hub navigation, shop, hangar, progression claims, and level-select actions; the remaining gate is live Unity review and signoff |
| F16 | UI | UI Polish and Motion Stack | Implemented (awaiting review) | DOTween is the approved UI motion layer, and the motion hooks are wired across hub transitions, overlays, modals, nav emphasis, and badge feedback. Runtime Unity verification remains the final gate. |
| F17 | Meta | Expanded Content Catalog | In progress | Ship customisation now builds on the authored shop ids with a preview-first hangar flow, and remaining work is runtime verification plus broader future category expansion |
| F18 | Meta | Long-Term Progression Economy and Lab Upgrades | Implemented (awaiting review) | The progression milestone now routes lab progression through the hub with purchase-refresh hooks and shared profile ownership; the authored six-row lab roster is now the closeout slice and only runtime review remains |
| F19 | Meta | Achievements and Challenge Tiers | In progress | A five-track achievement shell, authored config, progress evaluation, and persistent tier-claim flow are now in repo through the existing profile metrics; live Unity validation remains |
| F20 | Meta | Pre-Run Boosters | Implemented (awaiting player review) | One-per-family loadout persistence and run-scoped shield, coin, and score effects are wired and live-verified; only player-facing clarity review remains |
| F21 | UI | FTUE Tutorial | Implemented (awaiting player review) | The save-backed v3 sequence now teaches the core run only, provides deterministic pickup assists, waits for real crash/Continue actions, and has focused PlayMode coverage |

## Review Notes

- Treat `In progress` as the current active bundle work, not a promise of single-feature exclusivity. The repo currently carries approved bundles across `F8`, `F10`, `F11`, `F12`, `F15`, `F16`, `F17`, `F18`, and `F19`; avoid starting additional features unless the user explicitly approves them.
- Treat `Implemented` as "present in repo and usable now", not "fully production complete".
- Treat `Partial` as "some implementation exists but the documented product goal is not complete".
- Experience rows may map to multiple canonical `F#` features when the player-facing slice spans gameplay, UI, and monetisation.
