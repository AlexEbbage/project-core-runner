# Implementation Plan Addendum — Step 4 Vertical Slices

The current implementation plan should now be read through the vertical-slice roadmap in:

```text
docs/verticals/vertical-roadmap.md
```

## Current next build target

The next build target is:

```text
Vertical 1 — Core Run MVP
```

Required player flow:

```text
Splash/Main Hub -> Play -> Run HUD -> dodge walls -> collect coins -> crash -> Game Over -> rewards -> retry/hub
```

## Do not start yet

Do not prioritise the following until their verticals are reached:

- full shop monetisation,
- separate achievement/task pages,
- deep cosmetic collections,
- commercial SDK wiring,
- closed-testing store metadata,
- large obstacle combinations beyond the first wall loop.

These should remain visible in docs but not drive implementation until the core run is fun.
